---
interface Link {
	href: string
	text: string
	id: string
}

const links: Link[] = [
	{ href: "#projects", text: "Proyectos", id: "projects" },
	{ href: "#technologies", text: "Tecnologías", id: "technologies" }
]
---

<nav class="absolute top-10 left-1/2 -translate-x-1/2 flex justify-center items-center h-8 z-[1000]">
  	<ul class="flex justify-center items-center gap-4">
    	{
			links.map(({href, text, id}) => (
					<li class="text-2xl text-white hover:bg-white hover:text-background-light dark:hover:text-black rounded-md transition-colors duration-200 ease px-2" data-id={id}>
						<a href={href}>{text}</a>
					</li>
			))
		}
  	</ul>
</nav>

<script>
</script>